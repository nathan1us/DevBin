const config = require('./config');
const models = require('../models');

const {
    pasteController,
    userController
} = require('../controllers');

module.exports = (app) => {
    // TODO: Implement routes
}   
